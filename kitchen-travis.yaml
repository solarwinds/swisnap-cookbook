---
driver:
  name: local

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-16.04

suites:
  - name: default
    run_list:
      - recipe[swisnap::install_dependencies]
      - recipe[swisnap::docker]
